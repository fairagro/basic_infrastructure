apiVersion: v1
kind: Pod
metadata:
  name: temporary-nextcloud-backup-pod
spec:
  serviceAccountName: nextcloud-backup-account
  containers:
    - name: nextcloud-backup
      image: zalf/fairagro_nextcloud_backup-3.12:0
      imagePullPolicy: Always
      command:
        - sleep
        - "600"
  restartPolicy: OnFailure
