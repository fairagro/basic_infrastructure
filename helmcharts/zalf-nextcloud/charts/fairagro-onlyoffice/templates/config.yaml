apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "fairagro-onlyoffice.config" . }}"
data:
  local.json: |
    {
      "services": {
        "CoAuthoring": {
          "sql": {
            "pgPoolExtraOptions": {
              "ssl": {
                "rejectUnauthorized": false
              }
            }
          }
        }
      }
    }
